# Data Objects Catalog — Spring PetClinic

> **Generated:** 2026-05-29  
> **Source:** `src/main/java/org/springframework/samples/petclinic/model/`  
> **Companion to:** `BUSINESS_RULES.md`

This catalog documents every core data transfer object and JPA entity in the system: its fields, types, constraints, ORM mappings, and which business rules consume or produce each field.

---

## Summary

| Entity | Table | Fields | Key Rules | Source |
|--------|-------|--------|-----------|--------|
| `BaseEntity` | _(superclass)_ | id | RULE-001, RULE-021, RULE-022 | `model/BaseEntity.java` |
| `NamedEntity` | _(superclass)_ | name | RULE-004 | `model/NamedEntity.java` |
| `Person` | _(superclass)_ | firstName, lastName | RULE-003 | `model/Person.java` |
| `Owner` | `owners` | address, city, telephone, pets | RULE-002, RULE-003, RULE-010, RULE-015, RULE-016, RULE-017, RULE-022, RULE-023, RULE-026 | `model/Owner.java` |
| `Pet` | `pets` | birthDate, type, owner, visits | RULE-004–007, RULE-011, RULE-023, RULE-027, RULE-030, RULE-033 | `model/Pet.java` |
| `PetType` | `types` | _(name only)_ | RULE-006, RULE-019 | `model/PetType.java` |
| `Visit` | `visits` | date, description, pet | RULE-008, RULE-009, RULE-024, RULE-034 | `model/Visit.java` |
| `Vet` | `vets` | specialties | RULE-012, RULE-013, RULE-014, RULE-020, RULE-025 | `model/Vet.java` |
| `Vets` | _(wrapper)_ | vets (List\<Vet\>) | RULE-020 | `model/Vets.java` |
| `Specialty` | `specialties` | _(name only)_ | RULE-012, RULE-036 | `model/Specialty.java` |

---

## BaseEntity

**Source:** `src/main/java/org/springframework/samples/petclinic/model/BaseEntity.java`  
**JPA:** `@MappedSuperclass` — not mapped to its own table; fields inherited by all entities

| Field | Java Type | Column | DB Type | Constraints | Notes |
|-------|-----------|--------|---------|-------------|-------|
| `id` | `Integer` | `id` | `INTEGER GENERATED BY DEFAULT AS IDENTITY` | PRIMARY KEY | `null` = new (unsaved); assigned by DB on INSERT |

**Key methods:**

| Method | Returns | Business Logic |
|--------|---------|----------------|
| `getId()` | `Integer` | Returns the DB primary key |
| `setId(Integer)` | `void` | Manual id assignment (used by `OwnerController.java:114` on update) |
| `isNew()` | `boolean` | `return this.id == null` — drives INSERT vs UPDATE (RULE-001, RULE-021) |

**Rules consuming this object:**
- RULE-001 — universal identity state machine
- RULE-021 — INSERT vs UPDATE decision
- RULE-022 — `id` blocked from web binding

---

## NamedEntity

**Source:** `src/main/java/org/springframework/samples/petclinic/model/NamedEntity.java`  
**JPA:** `@MappedSuperclass` — extends `BaseEntity`

| Field | Java Type | Column | DB Type | Constraints | Notes |
|-------|-----------|--------|---------|-------------|-------|
| `name` | `String` | `name` | varies per subclass | None (DB); `@NotEmpty` only on `Pet` via `PetValidator` | Shared by `Pet`, `PetType`, `Specialty` |

**Subtypes:** `Pet`, `PetType`, `Specialty`

---

## Person

**Source:** `src/main/java/org/springframework/samples/petclinic/model/Person.java`  
**JPA:** `@MappedSuperclass` — extends `BaseEntity`

| Field | Java Type | Column | DB Type | Constraints | Notes |
|-------|-----------|--------|---------|-------------|-------|
| `firstName` | `String` | `first_name` | `VARCHAR(30)` | `@NotEmpty` (app); no NOT NULL (DB) | RULE-003 |
| `lastName` | `String` | `last_name` | `VARCHAR(30)` or `VARCHAR_IGNORECASE(30)` | `@NotEmpty` (app); no NOT NULL (DB) | RULE-003, RULE-015; H2 stores case-insensitively |

**Rules consuming this object:**
- RULE-003 — owner required fields (firstName, lastName)
- RULE-015 — case-insensitive search on lastName (H2 only)

**Subtypes:** `Owner`, `Vet`

**⚠️ Gap:** No `@Size(max = 30)` — a 31-character name passes application validation but hits DB column limit.

---

## Owner

**Source:** `src/main/java/org/springframework/samples/petclinic/model/Owner.java`  
**JPA:** `@Entity @Table(name = "owners")` — extends `Person`  
**DB Table:** `owners`

### Fields

| Field | Java Type | Column | DB Type | App Constraint | DB Constraint | Notes |
|-------|-----------|--------|---------|----------------|---------------|-------|
| `id` | `Integer` | `id` | `INTEGER IDENTITY PK` | blocked by `@InitBinder` | PRIMARY KEY | Inherited from BaseEntity |
| `firstName` | `String` | `first_name` | `VARCHAR(30)` | `@NotEmpty` | none (nullable) | Inherited from Person |
| `lastName` | `String` | `last_name` | `VARCHAR_IGNORECASE(30)` | `@NotEmpty` | none (nullable) | H2 case-insensitive; RULE-015 |
| `address` | `String` | `address` | `VARCHAR(255)` | `@NotEmpty` | none (nullable) | RULE-003 |
| `city` | `String` | `city` | `VARCHAR(80)` | `@NotEmpty` | none (nullable) | RULE-003 |
| `telephone` | `String` | `telephone` | `VARCHAR(20)` | `@NotEmpty` + `@Digits(fraction=0, integer=10)` | none (nullable) | RULE-002, RULE-031; stored as String |
| `pets` | `Set<Pet>` | _(FK: `pets.owner_id`)_ | _(mapped)_ | — | FK constraint | `CascadeType.ALL`; RULE-023, RULE-026 |

### Key computed properties

| Method | Returns | Rule | Logic |
|--------|---------|------|-------|
| `getPets()` | `List<Pet>` (unmodifiable) | RULE-010 | Sorts `petsInternal` by `Pet::getName` case-insensitively; returns new sorted list each call |
| `addPet(Pet)` | `void` | RULE-023 | Adds pet to `petsInternal`; sets `pet.owner = this` (bidirectional link) |
| `getPet(String name)` | `Pet \| null` | RULE-005 | Case-insensitive exact-name lookup including unsaved pets |
| `getPet(String name, boolean ignoreNew)` | `Pet \| null` | RULE-005 | When `ignoreNew=true`: skips pets where `isNew()==true`; used in duplicate-name check |
| `toString()` | `String` | RULE-008 (security) | Emits id, firstName, lastName, address, city, telephone — PII in logs |

### ORM relationships

| Relationship | Type | Cascade | Fetch | Notes |
|-------------|------|---------|-------|-------|
| `pets` | `@OneToMany(mappedBy="owner")` | `CascadeType.ALL` | Default (LAZY in JPA) | RULE-026: deleting Owner cascades-deletes all Pets in JPA mode only |

### Rules consuming / producing Owner

| Rule | Field(s) | Direction |
|------|---------|-----------|
| RULE-002 | `telephone` | Validates |
| RULE-003 | `firstName`, `lastName`, `address`, `city`, `telephone` | Validates |
| RULE-005 | `pets` (via `getPet()`) | Produces duplicate check input |
| RULE-010 | `pets` | Produces sorted list |
| RULE-015 | `lastName` | Query input |
| RULE-016 | `lastName` | Query input (empty → all) |
| RULE-017 | `id` | Produces redirect target |
| RULE-018 | `lastName` | Search failure |
| RULE-021 | `id` | INSERT vs UPDATE |
| RULE-022 | `id` | Blocked from web binding |
| RULE-023 | `pets` | Lifecycle: addPet() |
| RULE-026 | `pets` | Lifecycle: cascade delete |

---

## Pet

**Source:** `src/main/java/org/springframework/samples/petclinic/model/Pet.java`  
**JPA:** `@Entity @Table(name = "pets")` — extends `NamedEntity`  
**DB Table:** `pets`

### Fields

| Field | Java Type | Column | DB Type | App Constraint | DB Constraint | Notes |
|-------|-----------|--------|---------|----------------|---------------|-------|
| `id` | `Integer` | `id` | `INTEGER IDENTITY PK` | blocked by `@InitBinder` | PRIMARY KEY | Inherited from BaseEntity |
| `name` | `String` | `name` | `VARCHAR(30)` | `PetValidator`: `StringUtils.hasLength()` | none (nullable) | RULE-004; `NamedEntity.name` |
| `birthDate` | `LocalDate` | `birth_date` | `DATE` | `PetValidator`: not-null check | none (nullable) | RULE-007, RULE-030; format `yyyy/MM/dd` |
| `type` | `PetType` | `type_id` | `INTEGER NOT NULL` | `PetValidator`: required on create only | FK + NOT NULL | RULE-006, RULE-033 |
| `owner` | `Owner` | `owner_id` | `INTEGER NOT NULL` | set via `owner.addPet()` | FK + NOT NULL | RULE-023 |
| `visits` | `Set<Visit>` | _(FK: `visits.pet_id`)_ | _(mapped)_ | — | FK constraint | RULE-027 |

### Key computed properties

| Method | Returns | Rule | Logic |
|--------|---------|------|-------|
| `getVisits()` | `List<Visit>` (unmodifiable) | RULE-011 | Sorts `visitsInternal` by `Visit::getDate` reversed; new sorted list on each call |
| `addVisit(Visit)` | `void` | RULE-024 lifecycle | Adds visit to `visitsInternal`; sets `visit.pet = this` (bidirectional) |
| `isNew()` | `boolean` | RULE-001, RULE-005, RULE-006 | Inherited; `id == null` |

### ORM relationships

| Relationship | Type | Cascade | Fetch | Notes |
|-------------|------|---------|-------|-------|
| `owner` | `@ManyToOne @JoinColumn(name="owner_id")` | none | EAGER | owner_id is NOT NULL in DB |
| `type` | `@ManyToOne @JoinColumn(name="type_id")` | none | EAGER | type_id is NOT NULL in DB |
| `visits` | `@OneToMany(mappedBy="pet")` | `CascadeType.ALL` | `EAGER` | RULE-027: deleting Pet cascades-deletes all Visits in JPA mode |

**⚠️ EAGER loading of `visits`:** Every time a Pet is loaded (including as part of loading an Owner), all its visits are loaded immediately. This is the root cause of the full-graph over-fetch described in the topology analysis.

### Rules consuming / producing Pet

| Rule | Field(s) | Direction |
|------|---------|-----------|
| RULE-004 | `name` | Validates |
| RULE-005 | `name`, `isNew()` | Validates |
| RULE-006 | `type`, `isNew()` | Validates |
| RULE-007 | `birthDate` | Validates |
| RULE-011 | `visits` | Produces sorted list |
| RULE-023 | `owner`, `owner_id` | Lifecycle |
| RULE-027 | `visits` | Lifecycle: cascade delete |
| RULE-030 | `birthDate` | Format binding |
| RULE-033 | `type` (update path) | Suspected defect |

---

## PetType

**Source:** `src/main/java/org/springframework/samples/petclinic/model/PetType.java`  
**JPA:** `@Entity @Table(name = "types")` — extends `NamedEntity`  
**DB Table:** `types`

### Fields

| Field | Java Type | Column | DB Type | App Constraint | DB Constraint | Notes |
|-------|-----------|--------|---------|----------------|---------------|-------|
| `id` | `Integer` | `id` | `INTEGER IDENTITY PK` | — | PRIMARY KEY | |
| `name` | `String` | `name` | `VARCHAR(80)` | — | none (nullable) | Display value only |

### Seed data (from `data.sql`)

| id | name |
|----|------|
| 1 | cat |
| 2 | dog |
| 3 | lizard |
| 4 | bird (or snake — varies by DB dialect) |
| 5 | hamster |
| 6 | snake (or bird) |

**Rules consuming PetType:**
- RULE-006 — required on Pet creation
- RULE-019 — fetched ordered by name; used to populate dropdown

**⚠️ Not cached:** `findPetTypes()` issues a DB SELECT on every form render. No `@Cacheable` applied.

---

## Visit

**Source:** `src/main/java/org/springframework/samples/petclinic/model/Visit.java`  
**JPA:** `@Entity @Table(name = "visits")` — extends `BaseEntity`  
**DB Table:** `visits`

### Fields

| Field | Java Type | Column | DB Type | App Constraint | DB Constraint | Notes |
|-------|-----------|--------|---------|----------------|---------------|-------|
| `id` | `Integer` | `id` | `INTEGER IDENTITY PK` | — | PRIMARY KEY | Inherited |
| `date` | `LocalDate` | `visit_date` | `DATE` | none (no `@NotNull`) | none (nullable) | RULE-009: defaults to today; RULE-034: no boundary check |
| `description` | `String` | `description` | `VARCHAR(255)` | `@NotEmpty` | none (nullable) | RULE-008 |
| `pet` | `Pet` | `pet_id` | `INTEGER NOT NULL` | set via `pet.addVisit()` | FK + NOT NULL | RULE-024 |

### Key lifecycle

| Event | Trigger | Effect |
|-------|---------|--------|
| Construction | `new Visit()` | `this.date = LocalDate.now()` — sets today's date automatically |
| `pet.addVisit(visit)` | `@ModelAttribute` in `VisitController` | Sets `visit.pet = pet` bidirectionally |
| `saveVisit()` — new | `VisitController.processNewVisitForm()` | INSERT; id assigned |
| `saveVisit()` — existing (JDBC) | Never triggered via UI | Throws `UnsupportedOperationException` |

### Rules consuming / producing Visit

| Rule | Field(s) | Direction |
|------|---------|-----------|
| RULE-008 | `description` | Validates |
| RULE-009 | `date` | Produces (defaults to today) |
| RULE-011 | `date` | Consumed by Pet.getVisits() sort |
| RULE-024 | all | Lifecycle: write-once policy |
| RULE-034 | `date` | Suspected defect: no boundary |

---

## Vet

**Source:** `src/main/java/org/springframework/samples/petclinic/model/Vet.java`  
**JPA:** `@Entity @Table(name = "vets")` — extends `Person`  
**DB Table:** `vets`

### Fields

| Field | Java Type | Column | DB Type | App Constraint | DB Constraint | Notes |
|-------|-----------|--------|---------|----------------|---------------|-------|
| `id` | `Integer` | `id` | `INTEGER IDENTITY PK` | — | PRIMARY KEY | Inherited |
| `firstName` | `String` | `first_name` | `VARCHAR(30)` | — | none (nullable) | Inherited from Person; no validation (read-only entity) |
| `lastName` | `String` | `last_name` | `VARCHAR(30)` | — | none (nullable) | Inherited from Person |
| `specialties` | `Set<Specialty>` | `vet_specialties` join table | `M:M` | — | FK to both tables | RULE-012, RULE-013 |

### Key computed properties

| Method | Returns | Rule | Logic |
|--------|---------|------|-------|
| `getSpecialties()` | `List<Specialty>` (unmodifiable) | RULE-012 | Sorts by name case-insensitively; new list each call |
| `getNrOfSpecialties()` | `int` | RULE-013 | `getSpecialtiesInternal().size()` |
| `addSpecialty(Specialty)` | `void` | — | Exists in model but never called from any controller; specialty list is effectively immutable at runtime |

### ORM relationships

| Relationship | Type | Cascade | Fetch | Notes |
|-------------|------|---------|-------|-------|
| `specialties` | `@ManyToMany` | none | `EAGER` | No cascade — specialty changes not persisted via ORM |

### Rules consuming / producing Vet

| Rule | Field(s) | Direction |
|------|---------|-----------|
| RULE-012 | `specialties` | Produces sorted list |
| RULE-013 | `specialties` | Produces count |
| RULE-014 | `specialties` (count=0) | Produces "none" display |
| RULE-020 | all | Lifecycle: cached in vets cache |
| RULE-025 | all | Lifecycle: read-only at runtime |
| RULE-036 | all | Seed data |

---

## Vets (JAXB wrapper)

**Source:** `src/main/java/org/springframework/samples/petclinic/model/Vets.java`  
**JPA:** Not an entity — no `@Entity` annotation  
**Purpose:** JAXB marshalling wrapper for the `/vets.xml` and `/vets.json` endpoints

| Field | Java Type | Notes |
|-------|-----------|-------|
| `vets` | `List<Vet>` | Populated by `VetController.getVets()`; annotated `@XmlElement` |

**Rules consuming Vets:**
- RULE-020 — vets cache result is wrapped in this object for the JSON/XML endpoints

**⚠️ Architecture note:** This class exists solely to satisfy JAXB's requirement for a root element. It has no business logic. It can be eliminated if the three `/vets` endpoints are consolidated to a single content-negotiation endpoint (see TOPOLOGY assessment).

---

## Specialty

**Source:** `src/main/java/org/springframework/samples/petclinic/model/Specialty.java`  
**JPA:** `@Entity @Table(name = "specialties")` — extends `NamedEntity`  
**DB Table:** `specialties`

| Field | Java Type | Column | DB Type | App Constraint | DB Constraint | Notes |
|-------|-----------|--------|---------|----------------|---------------|-------|
| `id` | `Integer` | `id` | `INTEGER IDENTITY PK` | — | PRIMARY KEY | Inherited |
| `name` | `String` | `name` | `VARCHAR(80)` | — | none | Display value; `NamedEntity.name` |

### Seed data

| id | name |
|----|------|
| 1 | radiology |
| 2 | surgery |
| 3 | dentistry |

**Rules consuming Specialty:**
- RULE-012 — sorted display in vet list
- RULE-036 — seed reference data

---

## DB Schema Quick Reference

```
owners      (id PK, first_name, last_name[IGNORECASE], address, city, telephone)
pets        (id PK, name, birth_date, type_id NOT NULL FK→types, owner_id NOT NULL FK→owners)
visits      (id PK, pet_id NOT NULL FK→pets, visit_date, description)
vets        (id PK, first_name, last_name)
specialties (id PK, name)
vet_specialties (vet_id NOT NULL FK→vets, specialty_id NOT NULL FK→specialties)
types       (id PK, name)
```

### Constraint coverage gaps

| Table | Column | DB Constraint | App Constraint | Gap |
|-------|--------|---------------|----------------|-----|
| `owners` | `first_name` | **none** | `@NotEmpty` | Bypass-able at DB level |
| `owners` | `last_name` | **none** | `@NotEmpty` | Bypass-able at DB level |
| `owners` | `address` | **none** | `@NotEmpty` | Bypass-able at DB level |
| `owners` | `city` | **none** | `@NotEmpty` | Bypass-able at DB level |
| `owners` | `telephone` | **none** | `@NotEmpty + @Digits(10)` | Bypass-able; DB column is VARCHAR(20) — wider than app max |
| `pets` | `name` | **none** | `PetValidator` | Bypass-able at DB level |
| `pets` | `birth_date` | **none** | `PetValidator` | No range check anywhere |
| `visits` | `description` | **none** | `@NotEmpty` | Bypass-able at DB level |
| `visits` | `visit_date` | **none** | **none** | Fully unconstrained — any date accepted |

### Foreign key cascade map

```
owners ─CascadeType.ALL(JPA only)─► pets ─CascadeType.ALL(JPA only)─► visits
              └──── fk_pets_owners (DB blocks delete if pets exist, JDBC)
                                       └──── fk_visits_pets (DB blocks delete if visits exist, JDBC)
vets ──(no cascade)──► vet_specialties ──► specialties
```
